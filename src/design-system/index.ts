// Junta de Agua — Design System (fuente única, §18-19).
// Punto de importación canónico para TODA la UI del rebuild.
// No importar primitivas por ruta profunda; usar `import { Button } from '@/design-system'`.
export * from './primitives';
export * from './utils';
export * from './hooks';

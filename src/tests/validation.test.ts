import {describe,it,expect} from 'vitest';import {maskIdentifier,normalizeIdentifier,normalizeName,subscriberSchema} from '../features/subscribers/validation';
describe('validaciones de abonados',()=>{it('normaliza tildes y espacios',()=>expect(normalizeName('  Carlos Humberto López González ')).toBe('CARLOS HUMBERTO LOPEZ GONZALEZ'));it('normaliza identidad',()=>expect(normalizeIdentifier('0801-1998-12345')).toBe('0801199812345'));it('protege identidad',()=>expect(maskIdentifier('0801199812345')).toContain('****'));it('rechaza formulario incompleto',()=>expect(subscriberSchema.safeParse({}).success).toBe(false));});

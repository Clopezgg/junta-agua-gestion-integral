import {useState} from 'react';import {Navigate} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';
export function Mfa(){const a=useAuth();const[code,setCode]=useState('');const[error,setError]=useState('');const[enrollment,setEnrollment]=useState<{factorId:string;qr:string;secret:string}|null>(null);if(!a.session)return <Navigate to="/login" replace/>;if(a.mfaVerified)return <Navigate to="/" replace/>;
 async function begin(){try{const e=await a.enrollMfa();setEnrollment({factorId:e.factor.id,qr:e.qr,secret:e.secret})}catch(x){setError(x instanceof Error?x.message:'No se pudo activar MFA.')}}
 async function verify(e:React.FormEvent){e.preventDefault();setError('');try{if(enrollment)await a.verifyEnrollment(enrollment.factorId,code);else await a.verifyMfa(code)}catch(x){setError(x instanceof Error?x.message:'Código inválido.')}}
 return <main className="auth"><form onSubmit={verify}><h1>{a.hasVerifiedFactor?'Segundo factor':'Activar autenticador'}</h1>{!a.hasVerifiedFactor&&!enrollment&&<><p>Esta cuenta debe activar un código temporal antes de entrar.</p><button type="button" onClick={begin}>Generar código QR</button></>}{enrollment&&<><p>Escanee este QR con Google Authenticator, Microsoft Authenticator o una aplicación TOTP.</p><img className="qr" src={enrollment.qr} alt="Código QR de autenticación"/><small>Clave manual: <code>{enrollment.secret}</code></small></>}{(a.hasVerifiedFactor||enrollment)&&<label>Código de seis dígitos<input inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))}/></label>}{error&&<div className="error">{error}</div>}{(a.hasVerifiedFactor||enrollment)&&<button>Verificar y continuar</button>}</form></main>}

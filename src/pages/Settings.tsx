import {useEffect,useState} from 'react';
import {Building2,Upload} from 'lucide-react';
import {getOrganizationAssetUrl,getOrganizationSettings,updateOrganizationSettings,uploadOrganizationLogo} from '../features/settings/service';
export function Settings(){const[form,setForm]=useState<Record<string,string>>({name:'',address:'',phone:'',email:'',rtn:'',receipt_footer:'',currency:'HNL',logo_path:''});const[logo,setLogo]=useState('');const[message,setMessage]=useState('');const[error,setError]=useState('');useEffect(()=>{void(async()=>{try{const x=await getOrganizationSettings();const next={name:x.name??'',address:x.address??'',phone:x.phone??'',email:x.email??'',rtn:x.rtn??'',receipt_footer:x.receipt_footer??'',currency:x.currency??'HNL',logo_path:x.logo_path??''};setForm(next);setLogo(await getOrganizationAssetUrl(next.logo_path));}catch(e){setError((e as Error).message)}})()},[]);async function save(){try{await updateOrganizationSettings(form);setMessage('Configuración institucional guardada y auditada.');setError('')}catch(e){setError((e as Error).message)}}return <main className="content"><div className="titlebar"><div><h1>Configuración institucional</h1><p>Datos oficiales utilizados en recibos, informes y comunicaciones.</p></div></div>{message&&<div className="notice">{message}</div>}{error&&<div className="error">{error}</div>}<section className="panel"><div className="settings-brand"><div className="logo-preview">{logo?<img src={logo} alt="Logo institucional"/>:<Building2 size={48}/>}</div><label className="upload"><Upload size={18}/>Cargar logo<input type="file" accept="image/jpeg,image/png,image/webp" onChange={async e=>{const f=e.target.files?.[0];if(!f)return;try{const path=await uploadOrganizationLogo(f);setForm({...form,logo_path:path});setLogo(await getOrganizationAssetUrl(path));setMessage('Logo cargado. Guarde la configuración para confirmarlo.')}catch(err){setError((err as Error).message)}}}/></label></div><div className="form-grid"><label>Nombre oficial<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>RTN<input value={form.rtn} onChange={e=>setForm({...form,rtn:e.target.value})}/></label><label>Dirección<input value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label>Teléfono<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Correo<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Moneda<input value={form.currency} onChange={e=>setForm({...form,currency:e.target.value.toUpperCase()})}/></label><label className="span-2">Pie del recibo<textarea value={form.receipt_footer} onChange={e=>setForm({...form,receipt_footer:e.target.value})}/></label></div><button onClick={()=>void save()}>Guardar configuración</button></section></main>}

import { useState } from 'react'; import { Navigate } from 'react-router-dom'; import { useAuth } from '../contexts/AuthContext';
export function Login(){const a=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');if(a.session)return <Navigate to="/mfa" replace/>;return <main className="auth"><form onSubmit={async e=>{e.preventDefault();setError('');try{await a.signIn(email,password)}catch(x){setError(x instanceof Error?x.message:'No se pudo iniciar sesión.')}}}><h1>Acceso seguro</h1><p>Ingrese su cuenta individual.</p><label>Correo<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Contraseña<input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button>Ingresar</button></form></main>}

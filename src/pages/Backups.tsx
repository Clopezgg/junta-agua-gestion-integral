import {useCallback,useEffect,useState} from 'react';import {createBackup,downloadBackup,listBackups,restoreBackup} from '../features/backups/service';
export function Backups(){const[rows,setRows]=useState<any[]>([]);const[message,setMessage]=useState('');const[error,setError]=useState('');const load=useCallback(async()=>{try{setRows(await listBackups());setError('')}catch(e){setError((e as Error).message)}},[]);useEffect(()=>{void load()},[load]);return <main className="content"><div className="titlebar"><div><h1>Respaldos y restauración</h1><p>Copias cifradas y auditadas de la información de la organización.</p></div><button onClick={async()=>{try{setMessage('Creando respaldo…');await createBackup();setMessage('Respaldo creado.');await load()}catch(e){setError((e as Error).message)}}}>Crear respaldo</button></div>{message&&<div className="notice">{message}</div>}{error&&<div className="error">{error}</div>}<section className="panel"><div className="table-scroll"><table><thead><tr><th>Fecha</th><th>Estado</th><th>Tamaño</th><th>Checksum</th><th>Acciones</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString('es-HN')}</td><td>{r.status}</td><td>{r.size_bytes?`${Math.round(Number(r.size_bytes)/1024)} KB`:'—'}</td><td><code>{r.checksum_sha256?.slice(0,16)??'—'}</code></td><td><div className="actions">{r.status==='completed'&&<><button className="compact outline" onClick={async()=>{const x=await downloadBackup(r.id);window.open(x.url,'_blank','noopener,noreferrer')}}>Descargar</button><button className="compact outline" onClick={async()=>{const phrase=window.prompt('Escriba RESTAURAR para confirmar')??'';if(phrase==='RESTAURAR'){await restoreBackup(r.id,phrase);await load()}}}>Restaurar</button></>}</div></td></tr>)}</tbody></table></div></section><div className="notice">La restauración de aplicación conserva IDs y relaciones. Para recuperación completa del servidor también debe activarse el respaldo administrado de Supabase.</div></main>}
